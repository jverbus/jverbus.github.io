# Face detection, ONNX, and EIF revision notes

Implementation date: 2026-09-17. All 48 assigned IDs are `edited`; current locations and reasons are in `dispositions.json`. No source drift affected these edits. Each of the four complete revised articles was read for flow after applying the specified replacements; no additional prose changes were needed.

Every distinct original Markdown resource URL remains in its original article. Links formerly attached to source-as-narrator clauses now accompany the corresponding result, method, or historical claim. The ONNX website link moved to the opening sentence. The deleted ONNX section retains an empty `why-onnx` anchor before the converter section. Renamed headings retain their original fragment IDs, including the existing face-attribution anchor. The original fenced code and equations, figure markup, captions, and benchmark table rows were compared exactly and remain unchanged. The four edited posts are dated 2026-09-17; titles and publication routes are unchanged.

Retained methodological conditions prevent specific misunderstandings: the 2023 face result is scoped to StyleGAN-family images, preserves the Stable Diffusion failure, and distinguishes the CNN comparator's 3.3% false-positive rate from the 1% result. The 2024 article retains held-out generator counts, the Midjourney result, the zero synthetic-non-face true-positive rate, the model's historical replacement, separately trained resolution-matched models, JPEG operating points, and the face/non-face training confound. These prevent applying one model's operating point to a different population, resolution, or mechanism. Conditions recur in the figure caption only where needed to identify the plotted curves.

ONNX retains feature order, numeric input type, version matching, required tree-ensemble operators, the six-feature integration-test scope and assertion, and the unsupported EIF model family. These explain the actual format and executable model requirements. EIF retains level-zero differences in retry behavior, intercept sampling, and random-number consumption beside the parameter definition; the later duplicate was deleted. The dataset regressions, separate Ionosphere sweep, zero-size-leaf mismatch, precision failure, AI-assistance disclosure, and distinction between checked-in tests and a separate local study remain. The full local-study script and configuration are unavailable in the reviewed materials; this evidence limit is recorded only here and in the disposition report. It does not establish that the author never created or recorded them, and no new experiment or independent reproduction is claimed.

The explicitly targeted reviewer commentary, speculative controlled-experiment sentence, redundant validation conclusion, hypothetical ONNX deployments, and public local-study audit warning were deleted. The retained geometric contrasts, parameter list, benchmark table, and conditional ONNX requirement explain concrete methods or results. Untargeted figure captions and technical prose were preserved.

Focused source checks passed for URL sets, code/equations, image markup, captions, table rows, and `git diff --check`. Whole-site builds, rendered anchor checks, Castle checks, and browser inspection are performed by the coordinating agent after all parallel edits finish.
